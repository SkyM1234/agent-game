# AI 格斗竞技场开发文档

当前规则版本为 0.6.0，回放格式为 3。运行方式见 [README.md](README.md)。开发和测试使用 Conda 环境 `llm`；服务启动、停止与重启由使用者自行操作。

## 目标与范围

两个独立 agent 控制二次元像素人物，在水平擂台中同步决策。人物包提供不同的技能、提示词、模型工具、动画和特效；工具可用性随体力、魔力、回合冷却和行动状态变化。

当前人物为剑士「绯刃」与法师「霜铃」，支持脚本对战、脚本与 LLM 对战、双 LLM 对战、实时观战和回放。不实现长期记忆、复杂物理、角色成长、多人联网或多 agent 编排框架。

## 规则与状态

- 生命值、体力、魔力默认上限均为 100；生命值归零判负，同一步双方归零为平局。超时比较剩余生命值。
- 技能开始时一次性扣除 `stamina_cost` 和 `mana_cost`。资源不足时拒绝调用并休息保底；空挥不返还消耗。
- 调息/冥想通过人物技能的 `stamina_restore`、`mana_restore` 配置完整施放一次的恢复量，默认 15 体力、20 魔力，在 500ms 生效期内按进度结算并限制在上限内。前摇和后摇不恢复。字段省略或为 `null` 时使用全局每秒恢复率，显式 0 表示不恢复。
- 普通移动、休息必须零消耗、零冷却，在未被行动锁定时始终可用。
- 不再保留部位耐久、部位目标、受伤中断或伤势导致的技能禁用。
- 攻击自动针对对手，只削减生命值；攻击函数没有 `target` 参数。步行/疾步使用 `direction=forward/backward`；霜铃传送使用绝对坐标 `position`。
- 决策锁定时长为 `windup_ms + recovery_ms`，不包含 `active_ms`。锁定结束后新动作立即启动，尚未结束的旧动作生效效果并行结算。命中根据角色中心距离判定，单次攻击最多命中一次，飞行弹道使用路径交叉检查。
- 格挡减免正面伤害，疾步和传送没有无敌效果。普通移动不能穿过对手或推动静止对手；传送可越过对手但不能落在其占位范围或擂台边界外。
- 每人最多携带两种主动消耗品。道具使用占用行动并消耗一次次数；恢复受资源上限限制，护盾按回合到期并先于生命吸收伤害，烟雾弹只后撤且没有无敌效果。

传送复用 `dash` 引擎动作，通过 `teleport: true` 指定行为，要求零速度，前摇可自由配置。全部动作先按同一快照验证；双方落点若相互重叠，两次传送都拒绝并休息，不扣费。合法传送在前摇结束、动作进入 active 阶段时一次性更新位置与朝向，记录 `teleported` 事件的 `origin` 和 `destination`，不会产生路径上的移动或碰撞；零前摇保持动作启动时立即生效。

模型工具为 `frost_teleport(position)`，数值 schema 动态包含边界及排除对手占位的区间。解析层拒绝字符串、布尔、非有限值、越界和占位冲突。人物工具参数与传送配置必须匹配；已有回放保持自己的配置快照。前端禁止对传送跳变做移动插值，起点和终点冰环特效按回放时间重建。

## 回合与模拟时间

一次 `Arena.advance()` 是一个双方同步回合。引擎默认按 50ms 模拟步推进至 500ms 决策间隔，效果可以持续多个回合；双方都锁定时仍推进回合。只要当前动作累计经过的时间达到 `windup_ms + recovery_ms`，角色就在下一个回合决策点参与决策，新动作立即启动。

`cooldown_turns=N` 表示施放后的 N 个回合不能再次使用。第 1 回合使用冷却 2 回合的技能，第 2、3 回合不可用，第 4 回合恢复。动作锁定和冷却独立约束技能；冷却从施放开始记录，施放回合内界面最多显示配置的 N 回合。对局结束时保留实际结束回合编号。

模型等待、暂停、播放速度均不推进冷却。`active_ms` 控制效果持续时间，不增加决策锁定；旧效果可与新动作并行。技能冷却只接受非负整数回合。

每个回合从同一世界状态生成双方观察，并发收集需要的新动作，统一校验后推进模拟。同一步内先收集双方命中，再统一应用伤害，避免处理顺序偏袒一方。

## 人物包与工具调用

权威人物配置位于 `configs/characters/<character_id>/`：

- `character.json`：人物版本 `version` 和技能数值，包含魔力成本和回合冷却；版本加载到 `agent.version` 用于对局快照与提示词版本记录。
- `tools.json`：人物工具名、引擎动作映射和参数 schema。
- `prompt_neutral.md`：原版人物战术提示词，无行为偏好，默认使用。
- `prompt_aggressive.md`：激进型人物战术提示词，评测时通过 `--p1-prompt`、`--p2-prompt` 分别选择双方的 `neutral` 或 `aggressive`；`--prompt-variant` 设置双方公共默认值，省略时为 `neutral`。

人物表现素材位于 `frontend/public/assets/characters/<character_id>/`，包含 `animations.json`、`sprites/`、`effects/` 和 `portrait.png`。配置与素材通过 `character_id` 关联，后端不从前端静态目录读取规则。

新对局动态加载人物包并保存配置快照，修改人物文件后仅影响新对局。内部动作仍为 `jab`、`heavy_punch`、`kick`、`guard`、`dash`、`move`、`rest`；模型调用 `blade_flurry`、`ice_bolt` 等人物工具，后端验证并映射到动作。

模型请求把上下文拆成稳定和动态两部分。system 消息保存双方人物、武器、资源上限、有效技能、道具规则和人物提示词；user 消息包含双方当前资源、位置、动作阶段、剩余冷却、来袭攻击、当前回合及最近 4 个决策周期的增量。更早记录增量累计为 `opponent_history`，统计对手动作、方向、道具、结果和造成的伤害，不保留顺序且不无限增长；该状态随检查点恢复。历史 `decision_summary` 和不可用工具列表不回填，API 的 Tool Schema 只暴露当前合法技能。system prompt 在创建对局时进入头部快照，恢复对局不受代码中的新提示词影响。

每次调用一个工具并附最多 60 字的中文 `decision_summary`；摘要缺失或格式错误不影响合法动作。不持久化模型内部推理。密钥只从根目录 `.env` 读取，不发往前端或保存到回放。无效动作可在同一超时预算内修正一次；反馈会指出缺失与多余参数、枚举值、数值类型、上下界，以及传送排除角色占位后的合法区间。仍失败则休息保底；连续请求失败停止对局，保留明确错误原因。

反事实候选发送给模型前会转换为当前候选集合内的 0～100 相对偏好，并按动作名排列，不发送 `recommended_action` 或原始内部效用。只有权威推演确认所有对手应对都以同一种击倒结果结束时，才直接执行引擎最高收益动作并跳过模型；普通分差和时间到判定不会触发该捷径。

## 技术边界

| 模块 | 实现 | 职责 |
| --- | --- | --- |
| 规则引擎 | Python | 权威状态、动作阶段、命中与胜负判定 |
| 数据模型 | Pydantic | 工具参数、事件、资源和配置校验 |
| 对局服务 | FastAPI + WebSocket | 创建对局、并发决策、推送状态和播放确认 |
| Agent 适配 | 显式决策循环 + HTTP 客户端 | 动态工具调用、预算与异常处理 |
| 前端 | React + TypeScript + PixiJS | 界面、精灵和回放控制 |
| 存储 | SQLite/MySQL + JSONL + `.log` | 数据库保存恢复状态，JSONL 保存回放，`.log` 保存收益矩阵、脱敏后的模型决策上下文与结果 |

引擎不能依赖模型或浏览器，必须能够无界面运行。新增策略实现 `decide(observation)` 并返回 `Action` 或合法字典，无法直接修改权威状态。

## 前端与回放

界面展示人物、特效、生命、体力、魔力、技能状态与战术摘要。冷却展示为 N 回合，所有技能使用点击展开/收起的详情，支持键盘与手机操作，资源不足或冷却中的技能也能查看。详情包含消耗、冷却、动作时长及效果；移动距离为 `speed × active_ms / 1000`，恢复量为完整施放的体力和魔力恢复量。实际效果受碰撞、边界或资源上限限制。支持时间轴拖动、逐回合、暂停与倍速。

实时 WebSocket 每回合推送帧、事件与决策，浏览器播放完成后请求下一回合；取消不保存未完成对局。读取存档不执行 agent 或引擎。

格式 3 保存初始状态、配置摘要、人物提示词与工具版本、动作、事件、每步帧及最终结果。帧包含 `turn` 和双方 `mana`，`cooldowns` 为剩余回合。同一时刻可有多个帧，区分当前回合末、下一回合结算后与新动作提交后的状态。时间轴使用该时刻最后一帧，回到开头则显示初始状态。

已有 25 条旧回放已按要求清除，格式 1/2 不再支持。导入校验配置摘要、资源边界、时间及回合顺序、最终结果，并分配新 ID。

## 验证与后续评测

自动测试覆盖资源扣除、恢复上限、冷却边界、行动锁定、距离、碰撞、弹道、同步击倒、人物加载、工具过滤、回放、HTTP/WebSocket、并发、超时、保底和取消。可控模型响应不消耗真实 API 额度。

`python -m tools.check_turns_ui` 在进程内 API 和临时目录中检查构建后的前端，不启动服务、不写现有回放库；覆盖魔力不足、回合冷却、拖动、暂停及桌面/平板/手机布局，截图位于 `artifacts/turns/`。

`python -m tools.check_pixel_effects_ui` 使用固定脚本动作生成临时对局，检查两人 12 项技能、攻击残光、反向冰弹、暂停与重复拖动的一致性，以及四种屏幕宽度。特效总览与各技能截图位于 `artifacts/pixel-effects/`；同样不启动服务、不写现有回放库。

后续评测可从低魔力、不同冷却及距离的固定局面出发，比较策略的伤害交换、胜率、合法调用率、完成率、模型延迟和用量。交换出生位置、固定初始配置和请求预算，LLM 重复采样并报告样本量。动态工具过滤只能证明接口约束有效，策略表现需要对照实验。

待试玩调整：人物的魔力成本、恢复量、冷却回合、伤害、射程及动画阶段时长。已有双模型联调基线不替代新规则下的真实模型战术评测。
